if @serial 'lefthand' == @serial 'grindweap'
  stop
endif
if @findobject 'burstweap' 'any' 'backpack'
  @clearhands 'both'
  pause 600
  @equipitem 'burstweap' 2
  stop
endif
@removelist 'burstweaps'
@createlist 'burstweaps'
// halberd graphic
@pushlist 'burstweaps' 0x143e
for 0 to 'burstweaps'
  if @findtype burstweaps[] 'any' 'backpack'
    @setalias 'burstweap' 'found'
    if @findobject 'righthand' or @findobject 'lefthand'
      if graphic 'found' == burstweaps[]
        stop
      endif
      @clearhands 'both'
      pause 600
    endif
    @equipitem 'burstweap' 2
    stop
  endif
endfor

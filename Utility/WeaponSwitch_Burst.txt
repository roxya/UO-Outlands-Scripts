if not @listexists 'burstweaps'
  @createlist 'burstweaps'
else
  @clearlist 'burstweaps'
endif
// halberd, bardiche
@pushlist 'burstweaps' 0x143e
@pushlist 'burstweaps' 0xf4d
for 0 to 'burstweaps'
  if @graphic 'lefthand' == burstweaps[]
    stop
  endif
endfor
for 0 to 'burstweaps'
  if @findtype burstweaps[] 'any' 'backpack'
    @useobject 'found'
    stop
  endif
endfor
